#!/usr/bin/env bash
set -euo pipefail
SD="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"; PATH="$SD:$PATH"
. "$SD/log-lib"; . "$SD/zbx-lib"
jq -n '{output:["hostid","host"], selectInterfaces:"extend"}' | zbx_call host.get | jq -r '.result[] | [.hostid,.host]|@tsv'
