#!/usr/bin/env bash
set -euo pipefail
SD="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"; PATH="$SD:$PATH"
. "$SD/log-lib"; . "$SD/zbx-lib"
[ "${1:-}" = "-h" ] && { echo "Usage: $(basename "$0")"; exit 0; }
zbx_login && echo ok
