#!/usr/bin/env bash
set -euo pipefail
SD="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"; PATH="$SD:$PATH"
. "$SD/log-lib"; . "$SD/zbx-lib"
jq -n '{output:["templateid","name"], sortfield:"name"}' | zbx_call template.get | jq -r '.result[] | [.templateid,.name]|@tsv'
