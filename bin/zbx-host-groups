#!/usr/bin/env bash
set -euo pipefail
SD="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"; PATH="$SD:$PATH"
. "$SD/log-lib"; . "$SD/zbx-lib"
jq -n '{output:["groupid","name"], sortfield:"name"}' | zbx_call hostgroup.get | jq -r '.result[] | [.groupid,.name]|@tsv'
