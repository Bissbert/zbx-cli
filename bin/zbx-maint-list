#!/usr/bin/env bash
set -euo pipefail
SD="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"; PATH="$SD:$PATH"
. "$SD/log-lib"; . "$SD/zbx-lib"
jq -n '{selectHosts:"extend", output:"extend"}' | zbx_call maintenance.get | jq -r '.result[] | [.maintenanceid,.name,.active_since,.active_till]|@tsv'
